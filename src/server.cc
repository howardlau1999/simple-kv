#include <networking.h>
#include <btree.h>

int main() {
    // 1. Init Tree
    BTree *tree = new BTree(1024);
    // 2. Create Socket
    // 3. Bind Address
    // 4. Listen
    // 5. Server Loop
    for (;;) {
        
    }
}